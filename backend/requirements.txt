# ── Web Framework ──────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0

# ── Database ───────────────────────────────────────────
sqlalchemy==2.0.23
psycopg2-binary==2.9.9    # PostgreSQL driver (used in prod; SQLite is used as fallback)

# ── Auth & Security ────────────────────────────────────
python-jose[cryptography]==3.3.0
python-multipart==0.0.6

# ── HTTP / Google Fit ──────────────────────────────────
requests==2.31.0

# ── Data & ML ─────────────────────────────────────────
pandas==2.1.3
numpy==1.26.2
scikit-learn==1.3.2

# ── NLP / AI4Bharat IndicBERT / Transformers ──────────
torch==2.2.1
transformers==4.38.2
sentencepiece==0.1.99      # Required by many multilingual tokenizers

# ── LangChain (for AI Insight generation) ─────────────
langchain==0.1.0
langchain-community==0.0.10

# ── Scheduler ─────────────────────────────────────────
apscheduler==3.10.4

# ── Config & Utilities ────────────────────────────────
python-dotenv==1.0.0
pydantic==2.5.2
pydantic[email]==2.5.2
